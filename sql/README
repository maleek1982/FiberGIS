Предполагается наличие установленных PostgreSQL (8.4) и PostGIS (1.5.x)

Скрипт install_debian.sh использует следующие параметры:
DB=gis_2011
PG_SHARE_PATH="/usr/share/postgresql/8.4/contrib"

ВНИМАНИЕ: перед запуском install_debian.sh рекомендую ознакомиться с тем,
что скрипт будет делать. Крайне НЕ рекомендуется давать скрипту доступ к 
рабочему(не тестовому) серверу PostgreSQL.

Перед запуском скрипта необходимо выполнить:
su - postgres
